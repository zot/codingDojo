// Generated by CoffeeScript 1.6.2
/*
Copyright (C) 2013, Bill Burdick

(licensed with ZLIB license)

This software is provided 'as-is', without any express or implied
warranty. In no event will the authors be held liable for any damages
arising from the use of this software.

Permission is granted to anyone to use this software for any purpose,
including commercial applications, and to alter it and redistribute it
freely, subject to the following restrictions:

1. The origin of this software must not be misrepresented; you must not
claim that you wrote the original software. If you use this software
in a product, an acknowledgment in the product documentation would be
appreciated but is not required.

2. Altered source versions must be plainly marked as such, and must not be
misrepresented as being the original software.

3. This notice may not be removed or altered from any source distribution.
*/


/*
Tests for lists
*/


(function() {
  var L, Nil, assertEq, assertFail, cons, f, first, ifNoBrowser, len, list, rest, root, run, runTests, _i, _len, _ref;

  L = root = module.exports = require('./lists');

  require('./testing');

  list = L.list, cons = L.cons, Nil = L.Nil, first = L.first, rest = L.rest, len = L.len, run = L.run, runTests = L.runTests, assertEq = L.assertEq, assertFail = L.assertFail, ifNoBrowser = L.ifNoBrowser;

  ifNoBrowser(function() {
    return console.log('Testing...');
  });

  runTests({
    test1: function() {
      return assertEq("1", "1");
    },
    test2: function() {
      return assertEq(String(cons(1, Nil)), "List [1]");
    },
    test3: function() {
      return assertEq(String(list(1, 2, 3)), "List [1, 2, 3]");
    },
    test4: function() {
      return assertFail((function() {
        return first(Nil);
      }), "Not a list!");
    },
    test5: function() {
      return assertFail((function() {
        return first(1);
      }), "Not a list!");
    },
    test6: function() {
      return assertEq(first(list(1, 2, 3)), 1);
    },
    test7: function() {
      return assertEq(first(rest(list(1, 2, 3))), 2);
    },
    test8: function() {
      return assertEq(len(list(1, 2, 3, 4)), 4);
    }
  });

  ifNoBrowser(function() {
    return console.log('\nDone');
  });

  if (!L.stats.failures) {
    console.log("Succeeded all " + L.stats.successes + " tests.");
  } else {
    console.log("Succeeded " + L.stats.successes + " test" + (L.stats.successes > 1 ? 's' : '') + "\nFailed " + L.stats.failures + " test" + (L.stats.failures > 1 ? 's' : '') + ":");
  }

  _ref = L.stats.failed;
  for (_i = 0, _len = _ref.length; _i < _len; _i++) {
    f = _ref[_i];
    console.log("  " + f);
  }

  process.exit(0);

}).call(this);

/*
//@ sourceMappingURL=testLists.map
*/
